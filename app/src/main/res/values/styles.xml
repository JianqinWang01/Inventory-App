<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--Application head style-->
    <style name="toolBarTitle">
        <item name="android:textSize">26sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily"></item>
    </style>
   <!--<style name="myToolBar" parent="ThemeOverlay.AppCompat.Light">-->
       <!--<item name="actionBarItemBackground">@android:color/black</item>-->
   <!--</style>-->
    <style name="myTooBar" parent="AppTheme">
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="searchTool">
       <item name="android:width">60dp</item>
        <item name="android:height">60dp</item>

    </style>

    <style name="navigation_item_style">
        <item name="android:fontFamily">@font/anton</item>
        <item name="android:textSize">24dp</item>
        <item name="android:textColor">@android:color/black</item>

    </style>

    <!--Create style for searchView-->
    <!-- Base application theme. -->
    <style name="AppSearchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


</resources>
